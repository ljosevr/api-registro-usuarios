# ⚠️ Variables de Entorno - Plantilla de Configuración
#
# INSTRUCCIONES:
# 1. Copia este archivo: cp .env.example .env
# 2. Genera un secret seguro: openssl rand -base64 64
# 3. Reemplaza el valor de JWT_SECRET con el generado
# 4. NUNCA subas el archivo .env al repositorio (ya está en .gitignore)

# JWT Configuration
# Genera un nuevo secret con: openssl rand -base64 64
JWT_SECRET=GENERA-TU-SECRET-CON-OPENSSL-RAND-BASE64-64-Y-PEGALO-AQUI
JWT_EXPIRATION=86400000



